---
description: Project overview, tech stack, directory map and domain vocabulary for the Mabiao desktop app
alwaysApply: true
---

# 码表 (Mabiao)

桌面 GUI 工具：扫描本机各 AI 编程 CLI（Codex、Claude Code、pi、omp、dsh、opencode、kimi、gemini、grok、qwen、factory、copilot、hermes、cursor-agent…）留下的本地会话文件，归一化为「消耗记录」并聚合展示 token 用量与费用；另有独立的 Cursor「代码量」、官方额度、对话记录等维度。详见 `README.md` 和 `CONTEXT.md`。

## 技术栈

- 核心逻辑：Rust（`src-tauri/src`），Tauri 2 打包成原生桌面 App
- 界面：React 19 + TypeScript（strict）+ Vite 6 + ECharts，纯前端展示，不含业务逻辑
- 缓存：sqlite（`rusqlite`，bundled），可完全重建
- 依赖安装用 `pnpm`（不要用 `npm`/`yarn`）；`npm run tauri dev` / `npm run tauri build` 等 script 名保留但仍应通过 `pnpm run` 调用

## 目录结构

```
src-tauri/src/
  domain.rs / domain/     # 数据模型；根文件全量 re-export
  adapters/               # 每个 Usage Source 一个适配器
  conversation/           # 对话记录编排（子模块私有，根上 re-export）
  commands/               # Tauri command 函数体
  instructions/           # 全局指令扫描（实时读盘）
  official_quota/         # 官方额度（内置九家 + 自定义）
  report/                 # 报告 / 洞察（Rust）
  cursor_session.rs       # Cursor 会话 KPI 摄取
  cursor_account.rs       # Cursor 账号用量联网
  ingest.rs               # 扫描、可信摄取、归档对账
  store.rs / store/       # sqlite；ADAPTER_VERSION 在 store 根
  query.rs / query/       # 聚合 SQL（analytics / billing / …）
  aggregate.rs            # 内存基准，parity 测试用
  cost.rs                 # 费用推导
  user_files.rs           # 用户文件唯一写入入口（ADR 0010）
  backup.rs               # 备份 / 恢复
  work_timeline.rs        # 工作时间线
  tray.rs / tray_*.rs     # 菜单栏托盘
  lib.rs                  # generate_handler! + AppState
  bin/
    probe.rs              # 本机字段探测
    quota.rs              # 官方额度 CLI（mabiao-quota）
    litellm_snapshot.rs
    bench_ingest.rs
src/
  App.tsx
  components/
  hooks/                  # 前端 hooks（含 Cursor 账号自动刷新）
  report/                 # 周报 / 额度卡海报与注册表
  styles/                 # 分层 CSS（入口 styles.css）
  lib/
  types.ts
docs/adr/
```

对外路径约定见 `docs/adr/0017-rust-module-boundaries.md`。新增字段先改 `crate::domain::…`，不要在其它模块定义平行结构体。

## 领域词汇（详见 CONTEXT.md，请勿用其它措辞替代）

- **消耗记录 (Usage Record)**：归一化后的标准用量条目，唯一定义在 `domain::UsageRecord`
- **来源 (Source)**：被统计的 AI 工具（如 codex、claude），避免用「工具/tool/渠道」；权威名单是 `domain::Source::ALL`（14 个）
- **适配器 (Adapter)**：把某 Source 的原始格式解析成 Usage Record 的模块，避免用「parser/解析器」
- **Token 口径 (Token Dimension)**：input/output/cache_read/cache_creation/reasoning/total
- **代码量 (Code Volume)**：仅 Cursor 编辑器的 AI 生成代码行数统计，与 token 无关，界面上严格分区，避免与「用量/消耗」混用
- **官方额度 (Official Quota)**：账号级订阅限额；内置九家（Claude / Codex / Cursor / Grok / Droid / Antigravity / OpenCode / Copilot / Devin）加用户登记的自定义提供商。不进 UsageRecord / 本机 token KPI，不与本机 5 小时/7 天估计窗混条
- **Cursor 账号用量**：不进 UsageRecord / 本机 KPI / 5 小时窗；概览 7 天滚动可单独挂一行，缺价时用 LiteLLM 快照估算

## 关键架构约束

- 统计与界面代码只面向 `UsageRecord`/DTO，不感知任何具体工具的原始格式（`docs/adr/0001-adapter-per-source-architecture.md`）
- 前后端通过 Tauri command / IPC 交换已聚合的 JSON；webview 只做展示，不做聚合计算（`docs/adr/0002-tauri-rust-desktop-app.md`）
- 摄取缓存遵循「最后一次正确结果」语义；源文件消失时**归档**而非物理删除（`docs/adr/0003-trusted-ingestion-cache.md`、`0004-archive-missing-source-files.md`）
